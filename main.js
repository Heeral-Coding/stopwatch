function setup() {
    createCanvas(600, 600);
    angleMode(DEGREES);
}

function draw() {
    background(0);
    translate(300, 300);
    rotate(-90);

    // set up

    let hr = hour();
    let mn = minute();
    let sc = second();
    let ms = millis();

    
}